# Demo Data

This folder contains a small demo batch copied from the larger photo set in the workspace root.

Recommended demo command:

```bash
python main.py --input ./demo/input --output ./sample_output/inventory_demo.xlsx --csv ./sample_output/inventory_demo.csv --ocr auto
```

If you want to process the full local dataset instead, point `--input` to the workspace root.
